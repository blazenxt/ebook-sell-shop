{% comment %} TEST CLEAN v3 {% endcomment %}
<div class="bg-white border-b border-line">
  <div class="max-w-[1280px] mx-auto px-4 py-6 text-center">
    <div class="inline-flex bg-leafSoft text-leaf border border-leaf/10 px-3 py-1 rounded-full text-xs font-bold">TEST CLEAN — If you see this, sync works</div>
    <h1 class="font-serif text-3xl font-bold mt-4">{{ product.title }} — CLEAN</h1>
    <p class="text-muted mt-2">Price: {{ product.price | money }}</p>
    <div class="mt-4 flex justify-center gap-3">
      <span class="bg-ink text-white px-4 py-2 rounded-full">Ink</span>
      <span class="bg-gold text-ink px-4 py-2 rounded-full">Gold #C19A5C</span>
      <span class="bg-leaf text-white px-4 py-2 rounded-full">Leaf #18794E</span>
    </div>
  </div>
</div>
{% schema %}{"name":"Main Product TEST","settings":[]}{% endschema %}
