{% comment %} TEST CLEAN v3 MARKER2 + static img {% endcomment %}
<div class="bg-white border-b border-line">
  <div class="max-w-[1280px] mx-auto px-4 py-6 text-center">
    <div class="inline-flex bg-leafSoft text-leaf border border-leaf/10 px-3 py-1 rounded-full text-xs font-bold">TEST CLEAN MARKER2 — static img</div>
    <h1 class="font-serif text-3xl font-bold mt-4">{{ product.title }} — CLEAN v2 static</h1>
    <p class="text-muted mt-2">Price: {{ product.price | money }}</p>
    <img src="https://via.placeholder.com/300" alt="test" class="w-full h-auto mt-4">
  </div>
</div>
{% schema %}{"name":"Main Product TEST","settings":[]}{% endschema %}
