{% comment %} TEST CLEAN v3 MARKER2 {% endcomment %}
<div class="bg-white border-b border-line">
  <div class="max-w-[1280px] mx-auto px-4 py-6 text-center">
    <div class="inline-flex bg-leafSoft text-leaf border border-leaf/10 px-3 py-1 rounded-full text-xs font-bold">TEST CLEAN MARKER2 — If you see this, sync works v2</div>
    <h1 class="font-serif text-3xl font-bold mt-4">{{ product.title }} — CLEAN v2</h1>
    <p class="text-muted mt-2">Price: {{ product.price | money }}</p>
  </div>
</div>
{% schema %}{"name":"Main Product TEST","settings":[]}{% endschema %}
