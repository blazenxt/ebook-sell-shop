<div class="bg-white border-b border-line">
  <div class="max-w-[960px] mx-auto px-4 sm:px-6 lg:px-8 py-3">
    <div class="flex items-center gap-1.5 text-xs text-muted"><a href="{{ routes.root_url }}" class="hover:text-ink">Home</a> <i class="ri-arrow-right-s-line text-line"></i> <span class="text-ink font-medium">{{ page.title }}</span></div>
  </div>
</div>
<div class="max-w-[960px] mx-auto px-4 sm:px-6 lg:px-8 mt-4">
  <div class="bg-white rounded-2xl border border-line p-6 sm:p-8">
    <h1 class="font-serif text-2xl font-bold">{{ page.title }}</h1>
    <div class="prose max-w-none mt-4 text-sm leading-relaxed text-[#2b2b2b]">{{ page.content }}</div>
  </div>
</div>
{% schema %}{"name":"Main Page Clean","settings":[]}{% endschema %}
